{
  "project": "exp19b-commit-risk-adversarial-agentic-harness",
  "phase": "investigation-quality",
  "created": "2026-06-10T14:00:00Z",
  "schema_version": 2,
  "vision": "The V1 harness is built and validated: routing, context construction, schema validation, 6-dimension eval, cost governance, artifact persistence. The agent investigates commits and produces grounded evidence (D6=0.85). But it fails 3/6 acceptance gates — it can't reliably classify risk (D1=0.40) or identify root causes (D3=0.13) without the ground truth label. This phase focuses on improving investigation quality through systematic prompt engineering and eval iteration, not infrastructure changes.",
  "direction": "Iterative improvement cycle: change the prompt → run stratified eval → compare against thresholds → analyze failures → repeat. No infrastructure changes unless they unblock a metric. The harness is the control; the prompt is the variable.",
  "prior_phase": {
    "name": "v1-build",
    "archived_to": ".harness/archive/phases/v1-build/state.json",
    "summary": "11 tasks (scaffold → eval run). All done and committed. Built: GroundTruthGraph, GitContextProvider, CommitContextBuilder, AgentOrchestrator, XGBoostRouter, EvalHarness (6D), ReasoningJudge, CursorSDK provider, timestamped runs. 76 tests. Data leakage found and fixed post-V1 (buggy/fix labels).",
    "key_commits": ["c2341e2", "6ca573d", "731c088"]
  },
  "acceptance_thresholds": {
    "defined_at": "2026-06-10T11:45:00Z",
    "defined_before_run": "real_n100 (invalidated by data leak)",
    "min_eval_size": 50,
    "stratification": "50/50 buggy/clean",
    "gate": {
      "description": "All six must pass simultaneously. Blocks delivery.",
      "D1": 0.70, "D2": 0.15, "D3": 0.20, "D4": 0.60, "D5": 0.25, "D6": 0.60
    },
    "target": {
      "description": "Quality bar for an acceptable deliverable.",
      "D1": 0.80, "D2": 0.25, "D3": 0.35, "D4": 0.75, "D5": 0.40, "D6": 0.70
    },
    "stretch": {
      "description": "Excellence tier.",
      "D1": 0.90, "D2": 0.40, "D3": 0.50, "D4": 0.85, "D5": 0.55, "D6": 0.80
    },
    "baseline_rule": "soft — emit WARNING if D1_agent < D1_always_predict_clean or D1_router_only"
  },
  "current_baseline": {
    "run": "output/runs/2026-06-10_13-17-45_real_n5",
    "note": "First clean run after buggy/fix label leak fix. n=5 is too small for conclusions but establishes direction.",
    "scores": {
      "D1": 0.40, "D2": 0.08, "D3": 0.13, "D4": 0.84, "D5": 0.50, "D6": 0.85
    },
    "vs_gate": {
      "D1": "FAIL (0.40 < 0.70)",
      "D2": "FAIL (0.08 < 0.15)",
      "D3": "FAIL (0.13 < 0.20)",
      "D4": "PASS",
      "D5": "PASS",
      "D6": "PASS"
    }
  },
  "improvement_process": {
    "description": "Each improvement iteration follows this cycle. No code change without an eval to validate it.",
    "cycle": [
      "1. Hypothesize: which prompt change will move which dimension?",
      "2. Implement: change orchestrator.py system prompt or context assembly",
      "3. Smoke test: run n=5 to catch regressions quickly (~4 min, ~$0.02)",
      "4. Validate: run n=20 stratified eval to measure dimension scores (~20 min, ~$0.07)",
      "5. Compare: check against baseline and thresholds, analyze per-commit eval JSONs",
      "6. Decide: if gate-trending, run n=50 for statistical confidence; if flat, try different approach"
    ],
    "constraints": [
      "Oracle isolation must hold — agent never sees buggy/fix/year labels",
      "76+ tests must pass after every change",
      "No infrastructure changes unless they directly unblock a metric",
      "Each iteration tracked as a breadcrumb with before/after scores"
    ]
  },
  "failure_analysis": {
    "D1_why_failing": "Agent defaults to MEDIUM risk. Without the buggy label, it lacks a strong internal signal to push toward HIGH/CRITICAL. The XGBoost router already scored these commits 0.3-0.7 (gray zone), but the agent doesn't see this probability.",
    "D2_why_failing": "Agent lists files it analyzed (touched files from the diff) rather than files most likely to contain the bug. Jaccard penalizes extra files heavily.",
    "D3_why_failing": "Agent describes code structure changes accurately (D6=0.85 proves this) but cannot deduce the failure mechanism — it says 'this is complex' instead of 'this introduces a null pointer when X is empty'. Needs calibration examples showing the difference."
  },
  "tasks": [
    {
      "id": "iter-1",
      "name": "Prompt engineering: risk rubric + router probability + few-shot",
      "type": "feature",
      "status": "negotiating",
      "scope": "Add classification rubric to system prompt, inject XGBoost router_probability as context, add 2-3 few-shot examples. Run n=20 eval. Target: D1>=0.60, D3>=0.18.",
      "depends_on": [],
      "contract": "d1-d3-prompt-engineering"
    },
    {
      "id": "iter-2",
      "name": "Validate at n=50 if iter-1 trends toward gates",
      "type": "validation",
      "status": "pending",
      "scope": "Run n=50 stratified eval to confirm iter-1 improvements are statistically stable. All 6 gates must pass for delivery.",
      "depends_on": ["iter-1"]
    },
    {
      "id": "iter-3",
      "name": "Multi-turn investigation if D1/D3 still below gate",
      "type": "feature",
      "status": "pending",
      "scope": "Enable turn 2 for commits where confidence < 0.7. Turn 2 asks 'is this closer to LOW or HIGH?' and 'what specific failure mode could this introduce?'. Fallback if prompt-only doesn't reach gate.",
      "depends_on": ["iter-2"]
    },
    {
      "id": "iter-4",
      "name": "D2 localization focus",
      "type": "feature",
      "status": "pending",
      "scope": "Prompt the agent to distinguish 'files I analyzed' from 'files most likely to contain the defect'. May improve with D3 improvements.",
      "depends_on": ["iter-1"]
    }
  ],
  "risks": [
    {
      "risk": "Prompt changes improve D1 but regress D6 (agent starts guessing instead of grounding)",
      "mitigation": "D6>=0.70 is a hard constraint in every eval. Regression = revert.",
      "severity": "medium"
    },
    {
      "risk": "Few-shot examples cause parroting — agent copies example language instead of reasoning from actual diff",
      "mitigation": "EC-2 in contract: check 3 random eval reports for example language leakage",
      "severity": "medium"
    },
    {
      "risk": "Router probability injection creates a new form of leakage (agent uses P as proxy for buggy)",
      "mitigation": "Router is trained on train split only. P correlates with buggy but is not the label — same as any ML feature. Oracle isolation tests verify no GT fields leak.",
      "severity": "low"
    },
    {
      "risk": "n=5 baseline is too noisy — improvements may be sample variance",
      "mitigation": "iter-2 validates at n=50. Don't declare victory on n=20.",
      "severity": "medium"
    }
  ],
  "session_handoff": {
    "for_new_chat": "Phase: investigation-quality. V1 harness built (archived). Agent fails D1/D2/D3 gates after data leak fix. Current task: iter-1 (prompt engineering). Contract at .harness/contract.json (negotiating). Baseline: n=5 clean run. 76 tests. Read breadcrumbs for full trace.",
    "preserved_assets": [
      "All V1 code (orchestrator, eval_harness, router, ground_truth, etc.)",
      "data/apachejit/ + data/repos/ (Camel, Hadoop clones)",
      "output/runs/ (all run artifacts including invalidated ones)",
      ".harness/archive/phases/v1-build/ (prior state)"
    ]
  }
}
