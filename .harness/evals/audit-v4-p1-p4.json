{
  "audit_type": "post-commit-health-check",
  "timestamp": "2026-06-16T23:55:00Z",
  "packages_audited": ["models", "governance", "retrieval"],
  "commits_verified": {
    "v4-core-models": "b5fb124",
    "v4-governance-loaders": "50ce4e3",
    "v4-retrieval-module": "031757a",
    "v4-prompt-assembler": "04805de"
  },
  "computational_checks": {
    "pytest": "PASS — 291 passed, 0 failures (tests/ --ignore=tests/_v2_archive)",
    "file_sizing": "PASS — all .py in models/, governance/, retrieval/ ≤ 300 lines (max: prompt_assembler.py 294)",
    "circular_imports": "PASS — import commit_investigator.{models,governance,retrieval,prompt_assembler} succeeds"
  },
  "dimensions": {
    "cross_package_integration": {
      "result": "FAIL",
      "findings": [
        "prompt_assembler.py correctly calls load_rules(stage) and retrieve_skills(problem_statement, top_k=cfg.max_skills) at lines 92–93; seed rules/skills render in live examination prompt (continue_below_confidence hard bullet, parent_chain_examine soft text, spark_serde_blame skill for SPARK keywords).",
        "retrieve_candidates() produces CandidateSet with CandidateCommit fields matching models/candidates.py schema; to_dict/from_dict round-trip verified in live integration script.",
        "Manual cross-package smoke test PASSED: populated ProblemStatement → mock GitContextProvider → retrieve_candidates (3 commits, metadata keys correct) → assemble_prompt for planning/examination/attribution — no TypeErrors, prompts 1188–2183 chars, 5–10 sections each.",
        "BLOCKING: ProblemExtractor Level 1 (from_jira_issue/from_raw) leaves extracted_files, extracted_keywords, extracted_symbols empty. Live test: extractor output → retrieve_candidates triggers fallback_triggered=True with zero git-signal strategy hits — only recency_fallback path runs. Retrieval spike recall@100=0.45 assumed Level 1 regex signals; committed extraction does not populate them. P5 cannot wire prepare_investigation() without adding signal extraction first.",
        "BLOCKING: No committed test covering ProblemStatement → retrieve_candidates → assemble_prompt chain. test_retrieval.py uses FakeProblem; test_prompt_assembler.py mocks loaders and uses hand-built CandidateSet — integration gap will regress silently.",
        "InvestigationState.candidates_total is not auto-synced from len(candidate_set.commits); P5/harness must set it explicitly or examination candidate formatting will be wrong (examined_count capped against stale total)."
      ]
    },
    "package_isolation": {
      "result": "PASS",
      "findings": [
        "models/: zero imports from agent/, eval/, infra/, governance/, retrieval/ (grep verified).",
        "governance/: zero imports from agent/, eval/, infra/, retrieval/. Imports extraction/ (ProblemStatement), models/ (CandidateSet, InvestigationBrief, InvestigationState), internal governance modules, stdlib, yaml — expected after P4 prompt assembler.",
        "retrieval/: zero imports from agent/, eval/, governance/. Imports extraction/, models/, infra/git_context only.",
        "Dependency direction is acyclic: models ← governance ← (future harness); models ← retrieval ← infra; no circular imports on import smoke test."
      ]
    },
    "adr_fidelity": {
      "result": "PASS",
      "findings": [
        "Retrieval combined strategy: all four strategies implemented (file_log, keyword_grep, pickaxe, blame) in strategies.py with DEFAULT_STRATEGIES in retriever.py. Recency fallback when signal count < 10. No time-window strategy (correct per spike ADR).",
        "File path resolution via git.resolve_file_path and SHA normalization via normalize_sha/resolve_ref implemented in strategies.py (matches spike critical requirements).",
        "Signal-count ranking: (-len(strategies), best_rank, commit_id) in _rank_signal_candidates. Recency tiebreaker on equal signal counts NOT implemented — spike §P16 notes mention it but Q4 defers as untested; non-blocking.",
        "Prompt assembly §Q3: 10 logical sections rendered with correct relative order when present (verified by test_all_sections_present_in_order and live planning prompt: System Role → Skills → Stage Instructions → Problem Statement → Candidate Summary). Empty sections omitted (planning has 5/10 with seed data).",
        "Truncation cascade matches ADR: evidence FIFO pop (attribution) → candidate cap 10 → skills cap 1; problem_statement, hard_rules, stage_instructions preserved under tight budget (test_truncation_cascade + live spot-check).",
        "Seed governance: 3 YAML rules {min_suspects, parent_chain_examine, continue_below_confidence} and 2 active MD skills {spark_serde_blame, npe_null_check_removal} load from data/governance/ with ADR field values; parent_chain_examine.check is None.",
        "Intentional ADR deviation (contracted in P4 AC18): Problem Statement section uses Title/Description/Extracted-* format via _format_problem_statement, not ProblemStatement.to_prompt_text() which still emits '## Bug Report:' V3 header. P5 should not call to_prompt_text() when using PromptAssembler."
      ]
    },
    "code_quality": {
      "result": "FAIL",
      "findings": [
        "All module files ≤ 300 lines (PASS).",
        "Three functions exceed 50-line code-standards limit: retriever.py retrieve_candidates (59 lines), prompt_assembler.py assemble_prompt (58), prompt_assembler.py _build_prompt (56).",
        "No dead registry.py duplicate (removed after P2 fix). No commented-out code blocks in V4 packages. No unused-import linter run; manual review shows no obvious dead imports.",
        "Tests exercise behavior (ranking order, truncation caps, section headers) but rely heavily on MagicMock/FakeProblem rather than real GitContextProvider or ProblemExtractor — adequate for unit scope, insufficient for integration confidence."
      ]
    },
    "contract_completeness": {
      "result": "FAIL",
      "findings": [
        "eval-v4-retrieval-module.json: overall PASS 16/16 AC + 8/8 EC. Weak evidence: AC12 max_candidates trim test asserts count/ranks but not which SHAs were dropped (eval report acknowledges; spot-check confirmed correct behavior). Task scope in state.json promised 'n=20 eval cases measuring recall@100' — no such test exists in test_retrieval.py; 0.45 spike baseline never validated against committed module (only compute_recall_at_k unit tests on mocks).",
        "eval-v4-prompt-assembler.json: overall PASS 19/19 AC + 10/10 EC. Weak evidence: test_planning_seed_has_no_rule_sections mocks load_rules during assemble_prompt while asserting seed load_rules separately — wiring not tested in one call. test_truncation_cascade includes tautological assertion ('InvestigationBrief not in prompt or Stage Instructions in prompt') — Stage Instructions always present. AC19 seed integration mocks retrieve_skills; load_rules wiring verified live, retrieve_skills verified in separate live call but not in same unmocked assemble_prompt path.",
        "eval-v4-governance-loaders.json: PASS with noted register(rule_id, stage, check_fn) signature vs contract text omitting stage param — behavior correct, harness must account for stage arg.",
        "No cross-package contract or eval exists for P1–P4 wiring — gap this audit fills."
      ]
    },
    "state_consistency": {
      "result": "PASS",
      "findings": [
        "state.json: v4-core-models, v4-governance-loaders, v4-retrieval-module, v4-prompt-assembler status=committed; v4-input-pipeline through spike-level2-extractor status=pending (P5–P9).",
        "Git HEAD commits match audit table: b5fb124, 50ce4e3, 031757a, 04805de with messages aligned to breadcrumbs.jsonl checkpoint entries.",
        "No orphaned registry.py or stale governance duplicate files. Prior eval reports present at expected paths; no conflicting verified flags for P1–P4."
      ]
    }
  },
  "overall": "FAIL",
  "blocking_issues": [
    {
      "id": "BI1",
      "severity": "critical",
      "title": "ProblemExtractor Level 1 does not populate retrieval signals",
      "detail": "ProblemExtractor.from_jira_issue/from_raw returns empty extracted_files/keywords/symbols. retrieve_candidates() with this input executes zero git-signal strategies and falls back to recency only — cannot reproduce spike combined recall@100=0.45. P5 input pipeline must implement Level 1 regex extraction (or equivalent) before calling retrieve_candidates().",
      "affected_packages": ["extraction", "retrieval"],
      "p5_impact": "prepare_investigation() will silently degrade to recency-only retrieval without new extraction logic"
    },
    {
      "id": "BI2",
      "severity": "high",
      "title": "No recall@100 validation of committed retrieval module",
      "detail": "state.json v4-retrieval-module scope and retrieval-spike ADR cite n=20 eval recall@100=0.45. Committed test_retrieval.py has 28 mock-only tests; compute_recall_at_k is unit-tested but production retrieve_candidates() never run against ground truth on eval cases. P6 checkpoint (Retrieval Recall@100 gate 0.35) has no baseline proof the committed code matches spike.",
      "affected_packages": ["retrieval"],
      "p5_impact": "P6 checkpoint may fail without early diagnosis; risk of building harness on unvalidated retrieval"
    },
    {
      "id": "BI3",
      "severity": "medium",
      "title": "Missing committed cross-package integration test",
      "detail": "No pytest covers ProblemStatement → retrieve_candidates → assemble_prompt for all three stages. Ad-hoc evaluator script passed with manually populated ProblemStatement; this path is not regression-protected.",
      "affected_packages": ["governance", "retrieval", "models"],
      "p5_impact": "Integration regressions between P3/P4 will not fail CI until P5 manual wiring"
    }
  ],
  "non_blocking_observations": [
    "Recency tiebreaker on equal signal-count ranks not implemented (retriever.py _rank_signal_candidates); spike lists it but Q4 ADR defers empirical validation.",
    "create_default_registry() exists in rules.py but is not exported from governance/__init__.py — P7 harness will import from rules module or needs export.",
    "HardRuleRegistry.register requires (rule_id, stage, check_fn) — ADR prose omits stage parameter; seed stubs registered correctly.",
    "ProblemStatement.to_prompt_text() retains V3 '## Bug Report:' format; PromptAssembler deliberately uses ADR Title/Description format — do not mix both in P5.",
    "InvestigationState.candidates_total must be set by caller to match CandidateSet size for correct Stage 3 candidate_summary slicing.",
    "Three functions 56–59 lines exceed 50-line code-standards guideline; consider refactor during P5/P7 if touched.",
    "Prior per-task eval PASS marks are technically correct for negotiated contracts but several rely on mocks or spot-checks documented in eval adversarial_findings sections."
  ],
  "recommendation": "fix-before-continuing"
}
