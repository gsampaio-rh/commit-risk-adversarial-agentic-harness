{
  "task_id": "chore-0",
  "feature": "Harness cleanup for apachejit-predictive-eval phase",
  "problem": "Harness artifacts from two prior phases (pipeline-build, dataset-research-v2) clutter .harness/ root, making phase tracking ambiguous. New phase needs a clean slate.",
  "proposed_solution": "Archive prior state.json, contract.json, evals/, audits/, and breadcrumbs to .harness/archive/phases/. Reset breadcrumbs with handoff entry. Remove contract.json from root. Keep state.json as active apachejit-predictive-eval plan.",
  "acceptance_criteria": [
    "AC-1: .harness/archive/phases/dataset-research-v2/state.json exists and contains a valid JSON object with project or tasks field from the dataset-research-v2 phase",
    "AC-2: .harness/archive/phases/dataset-research-v2/contract-research-9.json exists and is valid JSON",
    "AC-3: .harness/archive/phases/dataset-research-v2/evals/ contains eval-feat-1.json, eval-feat-3.json, eval-research-9.json (3 files)",
    "AC-4: .harness/archive/phases/dataset-research-v2/audits/ contains at least one JSON file",
    "AC-5: .harness/archive/phases/pipeline-build/state.json exists and is valid JSON",
    "AC-6: .harness/archive/phases/breadcrumbs-pipeline-and-research-v2.jsonl exists with 60+ lines (full prior history)",
    "AC-7: .harness/breadcrumbs.jsonl has exactly 2 lines — handoff entry + chore-0 completion breadcrumb",
    "AC-8: .harness/breadcrumbs.jsonl first line contains 'phase_handoff' type",
    "AC-9: .harness/contract.json exists (this file — written for evaluation)",
    "AC-10: .harness/state.json project field is 'apachejit-predictive-eval'",
    "AC-11: .harness/state.json chore-0 status is 'verified'",
    "AC-12: No evals/ directory at .harness/ root (moved to archive)",
    "AC-13: git diff --name-only -- src/ tests/ produces empty output (no source code changes)",
    "AC-14: .harness/archive/ legacy subdirs (docs, fixtures, schemas) are untouched"
  ],
  "edge_cases": [
    "EC-1: Pipeline-build state.json was recovered from git history (commit e4b7ec4), not from disk — verify it's valid JSON with tasks",
    "EC-2: Dataset-research-v2 state.json was recovered from git (commit 9545d19) since planner already overwrote it — verify it's the correct phase"
  ],
  "verification_method": "Shell commands only — ls, cat, wc, python3 JSON validation, git diff. No file modifications. Check each AC by inspecting the filesystem.",
  "rubrics": ["code-quality"],
  "rubric_mode": "warn",
  "negotiation_rounds": 0,
  "phase": "verified",
  "verified": true
}
