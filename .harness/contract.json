{
  "task_id": "state-reorder-audit",
  "feature": "state.json task ordering and dependency restructure for iter-3-first track",
  "problem": "Previous ordering had iter-2-n20 as priority 1 blocker and FIX-JUDGE-INFRA/EXP-JUDGE-SWAP as priorities 1-2. None of these block any iter-3 refactor step. The real work (architectural redesign) was buried at priority 4+.",
  "proposed_solution": "iter-3a-extract becomes priority 1 with no deps. Judge scoring infra (FIX-JUDGE-INFRA + EXP-JUDGE-SWAP) deferred to after iter-3e, depends_on iter-3e. iter-2-n20 demoted to optional conditional (priority 16, no blocker).",
  "acceptance_criteria": [
    "AC-1: iter-3a-extract has priority=1 and depends_on=[]",
    "AC-2: iter-3a-spike has priority=2 and depends_on=[iter-3a-extract]",
    "AC-3: iter-3b-wire-gates has priority=3 and depends_on=[iter-3a-extract, iter-3a-spike]",
    "AC-4: iter-3c-bundle-inject has priority=4 and depends_on=[iter-3a-extract]",
    "AC-5: iter-3d-smart-diff has priority=5 and depends_on=[iter-3a-extract]",
    "AC-6: iter-3e-decompose-prompt has priority=6 and depends_on=[iter-3b, iter-3c, iter-3d]",
    "AC-7: FIX-JUDGE-INFRA has depends_on=[iter-3e-decompose-prompt] — NOT depends_on=[]",
    "AC-8: EXP-JUDGE-SWAP has depends_on=[iter-3e-decompose-prompt] — NOT depends_on=[]",
    "AC-9: iter-3-validate depends_on includes FIX-JUDGE-INFRA AND EXP-JUDGE-SWAP AND iter-3e (no duplicate key)",
    "AC-10: iter-2-n20 has status=conditional, trigger contains 'NOT a blocker', depends_on=[]",
    "AC-11: No circular dependencies in the depends_on graph",
    "AC-12: direction field reflects iter-3 as main track and judge infra as post-iter-3e"
  ],
  "edge_cases": [
    "EC-1: iter-3-validate must not have a duplicate depends_on key (JSON duplicate key silently drops first value)",
    "EC-2: Both FIX-JUDGE-INFRA and EXP-JUDGE-SWAP must have the same priority value (8) — if they have different priorities the parallel_with relationship is misleading"
  ],
  "verification_method": "Read state.json and verify each AC against the actual JSON values. Check for duplicate keys. Trace the depends_on graph for cycles. Verify the direction field narrative matches the task ordering.",
  "rubrics": ["architecture"],
  "rubric_mode": "warn",
  "negotiation_rounds": 0,
  "phase": "evaluating",
  "verified": true
}
