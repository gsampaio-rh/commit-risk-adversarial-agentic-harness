{
  "task_id": "tech-debt-cleanup-post-reorg",
  "feature": "Post-reorg tech debt cleanup: delete dead code, fix stale docs/scripts, prune unused deps, verify clean tree",
  "depends_on": ["src-package-separation"],
  "acceptance_criteria": [
    "delete-dead-python: calibrate_confidence.py, src/eval/forensics.py, src/eval/__init__.py deleted",
    "delete-v2-panel-test: tests/test_panel_smart_diff.py deleted",
    "fix-stale-docs: README, system-spec, eval-framework, project_domain.mdc path corrections — zero stale module paths",
    "fix-stale-scripts: run-smoke-eval.sh uses eval.run_eval; check-docs.sh deleted",
    "prune-unused-deps: xgboost, scikit-learn removed from pyproject.toml",
    "archive-stale-audit: .harness/audits/ deleted",
    "verify-clean: pytest tests/ all pass (165); zero old import paths in src/, tests/, scripts/, docs/, README.md, AGENTS.md, .cursor/rules/"
  ],
  "verification_method": "pytest tests/; rg stale path patterns; filesystem checks for deleted artifacts; AGENTS.md vs find src/commit_investigator; pyproject.toml hatch/pytest config; python -m commit_investigator.eval.run_eval --help",
  "phase": "verified",
  "verified": true,
  "verified": true,
  "evaluator_verdict": {
    "verdict": "PASS",
    "evaluated_at": "2026-06-16T20:00:00Z",
    "summary": "Session-final validation: all 10 checks PASS. 165/165 tests, imports OK, zero stale paths, dead artifacts removed, README/AGENTS aligned, 22 .py files.",
    "eval_report": ".harness/evals/eval-session-final-validation.json"
  }
}
